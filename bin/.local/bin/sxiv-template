#!/bin/bash
# Select images interactively, get their paths when done
sxiv -to . -r . | while read -r img; do
    echo "Selected: $img"
    # Do something with selected images (copy, move, process)
done
