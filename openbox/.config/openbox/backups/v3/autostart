#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

# If you want to use GNOME config tools...
#
#if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
#  /usr/lib/openbox/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null 2>&1; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &

# i3 autostart commands converted for Openbox
# dex --autostart --environment i3 &
# VBoxClient-all &
# sudo usermod -aG vboxsf $(whoami) &

dunst &
picom &
feh --bg-fill ~/Pictures/2.jpg &
~/.config/i3/Touchpad-setup.sh &
~/.config/i3/wacom-setup.sh &
# setxkbmap -option altwin:swap_ralt_rwin &
unclutter -idle 3 &
auto-cpufreq --daemon &
fcitx5 -d &
mega-sync &
xset s 2100 2100 &
xset dpms 2100 2100 2100 &
xss-lock --transfer-sleep-lock -- i3lock --nofork -c 000000 &
xautolock -time 35 -locker 'systemctl suspend' -notify 30 -notifier 'notify-send "Suspending in 30 seconds"' &
polybar &
