#!/bin/bash
# List all managed configs
echo "📦 Managed Dotfiles:"
echo "-------------------"
for pkg in */; do
    pkg=${pkg%/}
    if [ -d "$pkg" ]; then
        echo "  $pkg"
        find "$pkg" -type f -name "*" | head -5 | sed 's/^/    /'
    fi
done
